# Environment selector: dev | stage | prod
ENV=dev

# Storage / DB
# For development we use MongoDB by default (dev docker compose brings up `mongo`).
# Mongo image/tag to use for local stacks. Recommended: 8.2.4 (or 8.0.0 if needed).
MONGODB_IMAGE=mongo:8.2.4

MONGODB_URI=mongodb://mongo:27017
MONGO_DB_NAME=bio_ai_server_db
UPLOAD_DIR=./uploads
DEBUG=true

# FatSecret (image recognition + product lookup)
FATSECRET_CLIENT_ID=
FATSECRET_CLIENT_SECRET=

# Optional integrations
OPENAI_KEY=
SENTRY_DSN=

# Notes:
# - Copy this to `.env` for local development and fill secrets where needed.
# - Alternatively create `.env.stage` and `.env.prod` and set `ENV=stage` or `ENV=prod` in `.env` to switch.
