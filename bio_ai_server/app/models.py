"""Deprecated SQLAlchemy models.

This file contains legacy SQLAlchemy model definitions. The project has migrated to MongoDB and
these models are kept for historical reference only. Use the `app/db/mongodb.py` helpers and
collection-based patterns instead.
"""

# legacy models retained for reference

import datetime

# See `app/db/mongodb.py` and `app/routers/pantry.py` for the new MongoDB-based usage.

